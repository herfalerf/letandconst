// ES5 Global Constants
// var PI = 3.14;
// PI = 42; // stop me from doing this!

//ES2015 Global Constants
const PI = 3.14;

//Quiz
//What is the difference between var and let? = Var is function scoped, let is block scoped.  Var can be redeclared and let cannot.  Var is hoisted

//What is the difference between var and cosnt? = var is functio scoped, const is block scoped.  Var can be reassigned and redeclared and const cannot do either. Var is hoisted

//What is the difference between let and const? = let can be reassigned and const cannot.

//hoisting is when the variable is declared at the top of the scope it is defined in, prior to its initialization.
